import 'package:flutter/material.dart';

class PanchangScreen extends StatelessWidget {
  const PanchangScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('pancchang'),
    );
  }
}
